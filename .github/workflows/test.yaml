name: test
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: my-step
        run: |
          chmod +x ./index.html
          cat ./index.html
